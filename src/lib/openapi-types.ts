/**
 * Minimal OpenAPI 3.0 type for the spec object.
 * Just enough to type-check our spec without pulling in a full openapi-types package.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type OpenAPIV3 = Record<string, any>;
